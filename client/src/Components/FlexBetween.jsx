import { styled } from "@mui/system"; // Import - Note
import Box from "@mui/material/Box"; // Import - Note
export const FlexBetween = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
});
